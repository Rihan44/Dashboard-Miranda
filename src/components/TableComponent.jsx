import styled from "styled-components"

export const TableComponent = ({data, columns}) => {
    return(
        <>
            
        </>
    );
}

const Table = styled.table`
    display:g grid:
    gricol: $(props => props.columns)
`